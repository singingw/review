<div class="card text-center card-color">
  <div class="card-header card-header-color">
    {{> matcha-tabs route='feeds'}}
  </div>

  <div class="mx-5 mt-4">
    <h1 style="text-align: left; color: #102832;">最新動態</h1>
    <hr />
  </div>

  <div class="d-flex mx-5 mt-4">
    <div>
      <div class="card" style="width: 30vw;">
        <div class="card-header card-header-color"> 最新抹茶 </div>
        <div class="card-body card-color" style="height: 27vw; overflow-y: auto;">
          {{#each matcha}}
          <div style="text-align: left;">
            <h4 class="mb-3 text">
              <a class="link-primary" href="/matchas/matcha/{{this.id}}" style="color: #292b48;">{{this.name}}</a>
              <small class="fs-6">[{{this.Category.name}}]</small>
            </h4>
            <h5 class="mt-2 mb-3 text">
              <span class="text-secondary">
                <i class="fas fa-clock pr-2"></i>
                營業時間：
              </span>
              {{this.openingHours}}
            </h5>
            <h5 class="mb-3 text">
              <span class="text-secondary">
                <i class="fas fa-location-dot pr-2"></i>
                店家地址：
              </span>
              {{this.address}}
            </h5>
            <div>
              <small style="font-size:medium;">at {{relativeTimeFromNow this.createdAt}}</small>
            </div>
            <hr>
          </div>
          {{/each}}
        </div>
      </div>
    </div>

    <div>
      <div class="card" style="width: 30vw;">
        <div class="card-header card-header-color"> 最新評論 </div>
        <div class="card-body card-color" style="height: 27vw; overflow-y: auto;">
        {{#each matchaComment}}
          <div style="text-align: left;">
            <h4 class="text">
              <a class="link-primary" href="/matchas/matcha/{{this.Matcha.id}}" style="color: #292b48;">{{this.Matcha.name}}</a>
            </h4>
            <p class="my-1" style="white-space: break-spaces;">{{this.description}}</p>
            <a class="link-secondary" href="/users/{{this.User.id}}">{{this.User.name}}</a>
            <small>at {{relativeTimeFromNow this.createdAt}}</small>
            <hr>
          </div>
        {{/each}}
        </div>
      </div>
    </div>
  </div>
</div>